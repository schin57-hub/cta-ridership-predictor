version: "3.9"

services:
  dashboard:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data        # mount real data folder so CSV isn't baked into image
    environment:
      - STREAMLIT_SERVER_HEADLESS=true
    restart: unless-stopped
